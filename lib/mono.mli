val maximum_joltage : int list -> int list
(*
  To start I need a monotoniclly increating stack that I reverse at the end
  It must never exceed a length of 12
  as well as never be less than 12
  never pop we are out of skips

*)
